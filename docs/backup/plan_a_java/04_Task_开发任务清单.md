# 文瀛餐厅管理系统 — 开发任务清单 (Task)
## 方案 A：若依 RuoYi-Vue + Java

**版本**：v1.0  **日期**：2026-06-14  **总工期**：10 天

---

## 团队分工

| 成员 | 主责 | 备注 |
|------|------|------|
| 成员 A（后端/架构） | 若依搭建、数据库、菜品+食材+配方+库存后端 | 兼 D1 环境部署 |
| 成员 B（后端/业务） | 订单后端（含核心扣库存）、统计接口 | 兼测试 |
| 成员 C（前端） | 所有前端页面、ECharts 看板、联调 | 兼文档+PPT |

> 若只有 1 人独立开发，将以下任务按优先级顺序执行，D4 配方页和 D7 统计 SQL 是最花时间的两个点。

---

## 每日任务（按天）

### D1 — 环境搭建与基础框架

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 1.1 | 克隆 RuoYi-Vue 仓库，导入 IDE（IDEA） | A | 后端可启动，无报错 |
| 1.2 | 创建 MySQL 数据库 `ry-vue`，导入若依初始化 SQL（`sql/ry_20xx.sql`） | A | 若依自带表存在 |
| 1.3 | 执行业务表 `schema.sql`（8 张 t_* 表 + 演示数据） | A | `SHOW TABLES` 可见 8 张表 |
| 1.4 | 修改 `application-druid.yml` 连接本地数据库，后端启动成功 | A | 访问 `localhost:8080` 返回 JSON |
| 1.5 | 前端：`cd ruoyi-ui && pnpm install && pnpm run dev` | C | 访问 `localhost:80` 看到若依登录页 |
| 1.6 | 用 `admin/admin123` 登录，浏览若依内置功能（系统管理/代码生成） | A+C | 明白代码生成器在哪 |
| 1.7 | Git 仓库初始化，建分支 `dev`，约定命名规范 | A | 推送到远程（GitHub/Gitee） |

**当天产出**：前后端环境跑通，三个人能访问同一个若依后台。

---

### D2 — 代码生成 + 后端基础模块

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 2.1 | 用若依代码生成器导入 6 张表（t_dish/t_category/t_ingredient/t_inventory/t_stock_in/t_order） | A | 生成 zip 并下载 |
| 2.2 | 解压生成代码，合并到工程（domain/mapper/service/controller） | A | 编译无报错 |
| 2.3 | 在若依后台「系统管理→菜单管理」添加业务菜单（参考 TRD 第 5 节路由表） | A | 菜单可见 |
| 2.4 | 配置三个角色（ADMIN/MANAGER/STOCK_KEEPER）并分配菜单权限 | A | 不同账号登录看到不同菜单 |
| 2.5 | 创建演示账号：manager/manager123，stockkeeper/stock123 | A | 三角色均可登录 |
| 2.6 | 验证生成的菜品 CRUD 接口可通（Knife4j 或 Postman 测试） | B | GET /api/dish/list 返回 200 |

**当天产出**：脚手架+权限配置完成，基础 CRUD 接口自动生成并可用。

---

### D3 — 菜品 + 分类后端完善 & 前端菜品页

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 3.1 | 菜品上下架接口 `PUT /api/dish/changeStatus`（service 层补充状态校验） | A | Postman 测通 |
| 3.2 | 菜品分类 CRUD（代码生成 + 简单校验：名称不能重复） | A | 测通 |
| 3.3 | **前端：菜品管理页**（基于代码生成的前端骨架完善） | C | — |
| — | &nbsp;&nbsp;列表：支持按分类下拉筛选、名称搜索、状态筛选 | C | 筛选生效 |
| — | &nbsp;&nbsp;新增/编辑弹窗：分类下拉用 /category/list 接口 | C | 弹窗保存成功 |
| — | &nbsp;&nbsp;上下架：状态列用 el-switch，change 时调 changeStatus 接口 | C | 开关切换立即生效 |
| — | &nbsp;&nbsp;删除：二次确认弹窗 | C | 删除后刷新列表 |
| 3.4 | 联调菜品页：前后端跑通 | A+C | 增删改查均可用 |

**当天产出**：菜品管理模块完整可用。

---

### D4 — 食材 + 配方页（关键页面）

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 4.1 | 食材后端：新增时同步初始化 t_inventory 记录（`IngredientService.addIngredient`） | A | 新增食材后库存表出现对应行 |
| 4.2 | **配方后端**：`POST /api/recipe/save`（覆盖写：先删再插） | A | Postman 测通 |
| 4.3 | 配方后端：`GET /api/recipe/list?dishId=x` 查询某菜品配方 | A | 返回配方列表 |
| 4.4 | **前端：食材管理页**（列表+新增/编辑弹窗，较简单） | C | CRUD 跑通 |
| 4.5 | **前端：配方维护页**（重点，需手写） | C | — |
| — | &nbsp;&nbsp;左侧：菜品下拉选择，切换时加载该菜品配方 | C | 切换菜品配方联动 |
| — | &nbsp;&nbsp;右侧：动态行表格（食材下拉+用量输入+删行按钮） | C | 可增删行 |
| — | &nbsp;&nbsp;保存按钮：调 /api/recipe/save | C | 保存成功 |
| 4.6 | 联调配方页 | A+C | 配方保存后可重新加载 |

**当天产出**：食材档案 + 配方维护可用（最复杂的前端页面完成）。

---

### D5 — 订单后端（核心业务）

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 5.1 | **模拟下单接口** `POST /api/order/mock` | B | — |
| — | &nbsp;&nbsp;校验菜品上架状态 | B | 下架菜品下单返回错误 |
| — | &nbsp;&nbsp;生成订单号（"M" + 时间戳） | B | 订单号唯一不重复 |
| — | &nbsp;&nbsp;INSERT t_order + 批量 INSERT t_order_item | B | 数据库有记录 |
| 5.2 | **完成订单接口** `PUT /api/order/complete/{id}`（★最重要） | B | — |
| — | &nbsp;&nbsp;@Transactional 事务注解 | B | — |
| — | &nbsp;&nbsp;遍历明细→查配方→UPDATE t_inventory（带 stock>=deduct 条件） | B | 库存正确扣减 |
| — | &nbsp;&nbsp;库存不足时抛 ServiceException，验证事务回滚 | B | 回滚后库存不变 |
| — | &nbsp;&nbsp;订单状态改为 2 | B | t_order.status=2 |
| 5.3 | 退单接口 `PUT /api/order/cancel/{id}`（状态改 3，不扣库存） | B | 测通 |
| 5.4 | 订单列表 + 详情接口（代码生成已有，完善 join 查询返回明细） | B | 详情含 order_item |
| 5.5 | 单元测试：针对 completeOrder 写两个测试用例（正常扣减/库存不足回滚） | B | 测试通过 |

**当天产出**：订单核心逻辑（下单+扣库存）完成并测试通过。

---

### D6 — 订单前端 + 库存前端

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 6.1 | **前端：订单管理页** | C | — |
| — | &nbsp;&nbsp;列表：状态/时间区间筛选，el-tag 显示状态（不同颜色） | C | 筛选生效 |
| — | &nbsp;&nbsp;详情弹窗：展示订单信息 + 明细表格 | C | 弹窗正常 |
| — | &nbsp;&nbsp;**"完成订单"按钮**：只对 status=1 的订单显示 | C | 点击后库存变化 |
| — | &nbsp;&nbsp;**"模拟下单"弹窗**：多选菜品+输入份数（el-checkbox-group 或表格勾选） | C | 下单成功列表刷新 |
| 6.2 | **前端：库存管理页** | C | — |
| — | &nbsp;&nbsp;列表：库存低于 safety_stock 时该行标红（row-class-name） | C | 预警高亮显示 |
| — | &nbsp;&nbsp;"入库"弹窗：选食材、输数量和单价、备注 | C | 入库后库存数字变化 |
| 6.3 | 联调订单+库存页 | B+C | 完成订单→库存实时变化 |

**当天产出**：业务主闭环可完整演示（配方→入库→下单→完成→看库存变化）。

---

### D7 — 统计接口 + 看板后端

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 7.1 | `StatsMapper.xml` 中写 4 条聚合 SQL（今日营收、今日订单数、近7天、Top10） | B | 单独 SQL 在 MySQL 里验证结果正确 |
| 7.2 | `StatsService.getOverview()` 组装 4 个查询结果为一个 DTO | B | 返回 JSON 结构正确 |
| 7.3 | `GET /api/stats/overview` 接口，配置仅 MANAGER/ADMIN 可访问 | B | Postman 测通，返回 4 组数据 |
| 7.4 | 库存预警列表：`GET /api/inventory/warning`（stock <= safety_stock） | A | 返回预警食材列表 |

**当天产出**：统计接口完成，可以开始前端看板。

---

### D8 — 数据看板前端

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 8.1 | 看板页：2 个数字卡片（今日营收、今日订单数），样式美化（大字+彩色背景） | C | 数字正确显示 |
| 8.2 | 近 7 天营收折线图（ECharts，带渐变面积） | C | 图表正常渲染 |
| 8.3 | 销量 Top10 横向柱状图 | C | 图表正常渲染 |
| 8.4 | 库存预警模块（嵌入看板底部，展示低库存食材） | C | 预警列表显示 |
| 8.5 | 页面 resize 时 ECharts 自适应（`window.addEventListener('resize', chart.resize)`） | C | 缩放窗口图不变形 |
| 8.6 | 全站样式微调：系统名称改为"文瀛餐厅管理系统"，logo 可选 | C | 顶部/登录页名称正确 |

**当天产出**：系统核心功能全部完成，视觉效果到位。

---

### D9 — 联调测试 + 演示数据

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 9.1 | 全链路演示走一遍，记录所有 bug | 全员 | Bug 清单整理完 |
| 9.2 | 修复 P0 级 bug（功能不通、数据不对） | A+B | 主流程无阻断性 bug |
| 9.3 | 执行演示数据生成脚本：7 天内 200 笔订单，分布合理（早中晚高峰） | B | 折线图有起伏，Top10 有排名差异 |
| 9.4 | 库存补货至合理水平，确保演示时不会触发库存不足 | B | 库存量充足 |
| 9.5 | 三角色权限验证：MANAGER 看不到系统管理，STOCK_KEEPER 看不到订单 | A | 权限隔离正确 |
| 9.6 | 前端样式 review：表格宽度、弹窗大小、移动端不要求但 1280px 下不变形 | C | 视觉无明显问题 |

**当天产出**：系统稳定，演示数据丰富，可对外演示。

---

### D10 — 文档 + PPT + 彩排

| # | 任务 | 负责 | 完成标志 |
|---|------|------|---------|
| 10.1 | 需求文档（1-2页）：主要功能截图+说明 | C | 文档完成 |
| 10.2 | 系统设计文档（1-2页）：架构图+核心表结构+关键接口 | A | 文档完成 |
| 10.3 | 部署说明（1页）：克隆→建库→改配置→启动后端→启动前端，5 步 | A | 文档完成 |
| 10.4 | 用户手册（1页）：三角色各自可做什么，截图说明 | C | 文档完成 |
| 10.5 | 答辩 PPT：背景→功能演示路线→技术架构→核心亮点（扣库存）→总结 | C | PPT 完成 |
| 10.6 | **彩排第一次**：模拟答辩，限时 8 分钟，记录卡壳点 | 全员 | 找出问题 |
| 10.7 | 修复彩排发现的演示 bug / 数据问题 | A+B | 修复完成 |
| 10.8 | **彩排第二次**：完整流畅，控制在 8 分钟内 | 全员 | 流畅无卡顿 |

**当天产出**：验收材料齐全，答辩准备完毕。

---

## 关键风险预案

| 风险 | 触发时间 | 应对 |
|------|---------|------|
| 配方页前端太复杂 | D4 结束未完成 | 简化为"逐行 form 表单"替代动态表格 |
| 扣库存 bug 难调 | D5 | 先跑单元测试，隔离 service 层测试 |
| 统计 SQL 数据不对 | D7 | 先在 Navicat 直接跑 SQL 验证，再放代码 |
| 演示数据看板图太丑 | D9 | 手动调整订单时间分布，让图有起伏 |
| 彩排发现功能缺 | D10 | 只保主闭环，其余用"后续迭代"话术挡 |

---

## 验收清单

- [ ] 三角色分别登录，菜单权限互相隔离
- [ ] 菜品增删改查 + 上下架
- [ ] 食材档案增删改查
- [ ] 配方维护（至少 3 道菜有完整配方）
- [ ] 食材入库，库存增加
- [ ] 模拟下单成功，生成订单
- [ ] 完成订单，库存按配方精准扣减
- [ ] 库存不足时，完成订单返回错误提示（不扣减）
- [ ] 数据看板 4 图正常显示，数字准确
- [ ] 库存预警标红
- [ ] 源码可通过 README 5 步骤成功运行
