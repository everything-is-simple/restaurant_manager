# 文瀛餐厅管理系统 — 施工计划与当前进度

> **本文档为单一施工计划**，合并了原 `04_Task_10天施工任务清单.md` 与 `current_status_gap_report.md`。
> 状态截至 2026-06-20（D6 已完成并完成后端实测复核），每节明确标记 `[完成]` / `[进行中]` / `[待办]`。

---

## 1. 项目定位

中北大学文瀛餐厅后台管理系统（课程/答辩项目），基于 **RuoYi-Vue 3.9.2 经典版**二次开发。

- 定位：高校食堂后台管理
- 不做：点餐小程序、在线支付、供应商管理
- 演示主闭环：`配方维护 → 食材入库 → 模拟下单 → 完成订单 → 按配方扣库存 → 看板统计`
- 答辩亮点：完成订单按菜品配方自动扣库存，使用 `UPDATE ... WHERE stock >= deduct` + 事务保证并发不超扣

---

## 2. 总原则

- 唯一底座：`RuoYi-Vue 3.9.2` 经典版（Vue 2 + Element UI + Spring Boot 4.0.6 + MyBatis + MySQL 8 + Redis）
- 唯一文档根：`docs/plan_ruoyi_classic/`（[完成]）
- 唯一代码仓根：`G:\restaurant_manager\`（[完成] GitHub 远程 `everything-is-simple/restaurant_manager`）
- **唯一实际运行目录：`G:\restaurant_manager\RuoYi-Vue\`**（后端 Maven 聚合、前端 `ruoyi-ui`、餐厅业务代码均以此目录为准）
- 临时文件存放：`G:\restaurant_manager_tmp\`（项目纪律）
- 先跑通环境 → 生成器落地骨架 → **运行基线校准（代码/菜单/权限/路由对齐）** → 补业务逻辑 → 补前端 → 演示数据 → 答辩准备

---

## 3. 当前进度速览

| 阶段 | 状态 | 说明 |
|------|------|------|
| D1 环境与底座 | ✅ [完成] | JDK 17 / Maven 3.9.16 / MySQL 8 / Redis / Node 16 全部可用，admin/admin123 可登录 |
| D1 数据库落库 | ✅ [完成] | 8 张业务表（t_category/t_dish/t_dish_ingredient/t_ingredient/t_inventory/t_order/t_order_item/t_stock_in）+ 种子数据 |
| D2 代码生成与菜单落库 | ✅ [完成] | 6 模块 CRUD 骨架已并入；`menu_init.sql` 已落库（31 菜单/按钮 + 2 角色 + 2 测试账号） |
| D3 运行基线校准 | ✅ [完成] | 运行目录冻结、recipe/stats 占位页面补齐、权限命名策略确定（CRUD 保持默认，D6 添加语义化权限） |
| D4 菜品与分类联调 | ✅ [完成] | dish/category 页面 UX 修正（分类下拉/名称映射/上下架/配方入口），delFlag 隐藏，编译构建通过 |
| D5 食材、配方与订单明细模型 | ✅ [完成] | TDishIngredient/TOrderItem 全套 Domain/Mapper/Service/XML；新增食材自动建库存；配方查询/覆盖保存接口+前端页面；支持菜单独立进入选菜；recipe 权限点已对齐；编译构建通过 |
| D6 订单核心逻辑 | ✅ [完成] | 模拟下单/完成订单/退单接口；按配方扣库存+事务回滚；库存不足校验 UPDATE WHERE stock>=qty；前端模拟下单弹窗+完成/退单按钮；权限点对齐；编译构建通过 |

**本轮盘点结论（代码 + 数据库实查，状态更新至 2026-06-20）：**

**已核实：**
- ✅ 实际运行目录为 `G:\restaurant_manager\RuoYi-Vue\`
- ✅ 业务表已落库：`t_category/t_dish/t_dish_ingredient/t_ingredient/t_inventory/t_order/t_order_item/t_stock_in`
- ✅ `menu_init.sql` 已执行：数据库中已存在 31 个业务菜单/按钮、2 个角色（`MANAGER`/`STOCK_KEEPER`）、2 个测试账号（`manager`/`stockkeeper`）
- ✅ 6 个生成模块骨架已并入：`category/dish/ingredient/inventory/order/stockIn`；另有 2 个手写模块 `TDishIngredient`、`TOrderItem`（D5 完成全套 Domain/Mapper/Service/XML）
- ✅ 后端可编译，前端可生产构建
- ✅ 前端路由机制已校准：餐厅页面通过后端菜单动态路由加载，不依赖 `ruoyi-ui/src/router/index.js` 手工注册
- ✅ D6 已完成接口级实测：`mockOrder / completeOrder / cancelOrder` 已跑通；库存不足回滚已验证

**仍需解决（D6-D10 范围）：**
- ❌ 6 个生成的 `T*ServiceImpl` 全是裸 CRUD，**0 业务逻辑**（D5 已为 TIngredientServiceImpl 添加 @Transactional 自动建库存，其余 D6 实现）
- ✅ `recipe` 页面已实现完整配方维护功能，支持从菜品页带参进入，也支持菜单独立进入后选菜维护（D5 完成）
- ✅ TDishIngredient / TOrderItem 的 Domain/Mapper/Service 已手写完成（D5 完成）
- ✅ stockIn 权限与菜单不对齐问题已修复（补充 menu_id 2053-2057，分配给 ADMIN + STOCK_KEEPER）

---

## 4. 业务算法清单（按"扣库存闭环"主线排序）

| # | 算法 | 涉及表/模块 | 优先级 |
|---|------|------------|--------|
| 1 | 新增食材自动初始化库存（stock=0） | t_ingredient + t_inventory | P0 |
| 2 | 入库累加库存 | t_stock_in + t_inventory | P0 |
| 3 | 配方覆盖保存（按 dish_id 删除后批量插入） | t_dish_ingredient | P0 |
| 4 | 模拟下单（生成 order_no + 批量插入 order_item） | t_order + t_order_item | P0 |
| 5 | **完成订单按配方扣库存**（核心） | t_order + t_order_item + t_dish_ingredient + t_inventory | P0（答辩主菜）|
| 6 | 退单回滚库存 | t_order + t_order_item + t_dish_ingredient + t_inventory | P0 |
| 7 | 库存不足事务回滚保护 | t_inventory | P0 |
| 8 | 库存预警列表（stock < safety_stock） | t_inventory + t_ingredient | P1 |
| 9 | 看板统计（今日营收/订单数/7天趋势/Top10） | t_order + t_order_item + t_dish | P1 |

---

## 5. 10 天排期（含当前状态，已校准）

### D1 环境准备 [✅ 完成]

- 安装并验证 JDK 17、Maven、MySQL、Redis、Node 16
- 导入若依初始化 SQL（`ry_20260417.sql`）
- 导入业务表 `schema.sql`（8 表已落库）
- 修改数据库配置（root/root）
- 跑起后端（8080）和前端（80）

**完成标志：**
- ✅ 登录页可打开
- ✅ `admin/admin123` 可登录
- ✅ 6 个 `/restaurant/*/list` API 返回种子数据

### D2 代码生成与菜单准备 [✅ 完成]

- ✅ 在若依代码生成器中导入 6 张表
- ✅ 生成菜品、分类、食材、库存、入库、订单基础 CRUD
- ✅ 合并代码到**实际运行项目** `G:\restaurant_manager\RuoYi-Vue\`（Controller → `ruoyi-admin/.../web/controller/restaurant/`；Domain/Service/Mapper/ServiceImpl/Mapper XML → `ruoyi-system/`；Vue API + Views → `ruoyi-ui/src/`）
- ✅ 配置业务菜单（`menu_init.sql` 执行完成，31 个菜单/按钮已落库）
- ✅ 配置三角色和测试账号（MANAGER/STOCK_KEEPER 角色 + manager/stockkeeper 测试账号）
- ✅ 补齐占位页面：`restaurant/recipe/index`、`restaurant/stats/index`（D3 完成）
- ✅ 权限命名策略确定：CRUD 保持默认，D6 添加语义化权限

**完成标志：**
- ✅ 基础 CRUD 页面和接口骨架已生成
- ✅ 业务菜单已落库，占位页面已创建
- ✅ 三角色与测试账号已建，D10 联调时复核菜单隔离

### D3 运行基线校准 [✅ 完成]

- ✅ 冻结唯一运行目录：统一以 `G:\restaurant_manager\RuoYi-Vue\` 为开发、运行、验收根目录
- ✅ 补齐占位页面：`restaurant/recipe/index.vue`、`restaurant/stats/index.vue`（避免菜单 404）
- ✅ 校准文档描述：明确前端采用后端菜单动态路由，不要求在 `ruoyi-ui/src/router/index.js` 手工注册餐厅页面
- ✅ **权限命名策略确定**：
  - 当前 CRUD 权限保持生成器默认（`restaurant:stockIn:*`、`restaurant:order:add/edit/remove`）
  - D6 实现业务逻辑时，添加语义化权限（`restaurant:order:mock/complete/cancel`）与 DB 菜单对齐
  - `TStockInController` 保持独立模块，不合并到 inventory

**完成标志：** 运行目录唯一、菜单/页面已对齐、命名策略已确定并记录

### D4 菜品与分类联调 [✅ 完成]

- ✅ 校准并验收 `category/dish` CRUD 骨架可用性
- ✅ **dish/index.vue UX 修正**：
  - 分类列显示分类名称（不再显示原始 categoryId 数字）
  - 新增/修改弹窗中分类改为 el-select 下拉选择
  - 移除表单中 delFlag 字段（category/dish 两个页面均已移除）
  - 搜索区移除不合理的"售价"搜索，改为分类下拉筛选
  - 状态搜索改为"上架/下架"下拉
- ✅ **菜品上下架**：列表操作列增加 el-switch 开关，复用 PUT 接口更新 status
- ✅ **配方编辑入口**：操作列增加"配方"按钮，跳转到 `/restaurant/recipe?dishId=xxx&dishName=xxx`
- ✅ recipe/index.vue 占位页升级：可显示跳转来源菜品名称和 ID
- ✅ 后端 `mvn compile` 通过，前端 `npm run build:prod` 通过

**6 模块 CRUD 代码审查验收表（截至 2026-06-19）：**

| 模块 | Controller | 前端页面 | list | add | edit | remove | export | search | pagination | delFlag 已清理 | 备注 |
|------|-----------|---------|------|-----|------|--------|--------|--------|------------|---------------|------|
| 分类 | TCategoryController | category/index.vue | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ D4 清理 | — |
| 菜品 | TDishController | dish/index.vue | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ D4 清理 | D4 新增：分类下拉、上下架、配方入口 |
| 食材 | TIngredientController | ingredient/index.vue | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 待 D7 | safetyStock 搜索待优化 |
| 库存 | TInventoryController | inventory/index.vue | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 待 D7 | ingredientId 显示为数字，待改名称映射 |
| 入库 | TStockInController | stockIn/index.vue | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 待 D7 | ingredientId 显示为数字，待改下拉选择 |
| 订单 | TOrderController | order/index.vue | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 待 D7 | 需 restaurant_order_status 字典；D6 后重写业务逻辑 |

**完成标志：** ✅ 菜品与分类模块从"已生成骨架"提升为"可稳定演示"，配方入口可见

### D5 食材、配方与订单明细模型 [✅ 完成]

- ✅ 手写 `t_dish_ingredient` 全套：TDishIngredient Domain（含 ingredientName 非持久化 join 字段）、TDishIngredientMapper 接口 + XML（LEFT JOIN t_ingredient 回填食材名称、batchInsert 批量插入、deleteByDishId）、ITDishIngredientService 接口 + TDishIngredientServiceImpl 实现（含 saveRecipe @Transactional 先删后插）
- ✅ 手写 `t_order_item` 全套：TOrderItem Domain、TOrderItemMapper 接口 + XML（batchInsert 批量插入、deleteByOrderId）、ITOrderItemService 接口 + TOrderItemServiceImpl 实现
- ✅ 新增食材自动初始化库存：TIngredientServiceImpl.insertTIngredient() 增加 @Transactional，插入食材后自动创建 TInventory 记录（stock=0）
- ✅ 配方查询和覆盖保存接口：TRecipeController（GET /restaurant/recipe/list/{dishId}` 使用 `restaurant:recipe:query`；PUT /restaurant/recipe/save 使用 `restaurant:recipe:save`）
- ✅ 配方维护前端页面：recipe/index.vue（支持菜单独立进入后先选菜，也支持从 dish 页面带参进入；食材下拉选择 + 用量输入 + 新增/删除行 + 保存，含重复食材和 quantity > 0 校验）+ recipe.js API
- ✅ 后端 `mvn compile` 通过，前端 `npm run build:prod` 通过

**完成标志：** ✅ 菜品与食材建立可保存的配方关系，新增食材自动建库存，配方页面从菜单或菜品页进入都可编辑保存

### D6 订单核心逻辑 [✅ 完成]

- ✅ 模拟下单接口（生成 `order_no` 时间戳 + 批量插入 order_item + 状态=1）
- ✅ **完成订单接口**（事务 + 按配方扣库存 + 状态=2 + 写 complete_time）
- ✅ 退单接口（事务 + 回滚库存 + 状态=3）
- ✅ 库存不足校验：`UPDATE t_inventory SET stock = stock - #{qty} WHERE ingredient_id = #{id} AND stock >= #{qty}`，返回 0 则抛 ServiceException 触发回滚
- ✅ 前端联调：模拟下单弹窗（选菜品+份数）、完成订单按钮、退单按钮，按状态显示
- ✅ 权限点对齐：`restaurant:order:mock/complete/cancel`（后端 @PreAuthorize + 前端 v-hasPermi + menu_init.sql 已落库）
- ✅ 测试数据脚本：`G:\restaurant_manager_tmp\d6_test_data.sql`（重置环境 + 5 个测试场景说明）
- ✅ **2026-06-20 后端接口实测复核通过**：
  - 使用 `manager/admin123` 登录成功，验证码接口返回 `captchaEnabled=false`
  - `POST /restaurant/order/mock` 成功生成 `orderId=3`
  - `PUT /restaurant/order/complete/3` 成功，订单状态 `1 -> 2`，`complete_time` 正常写入
  - 按真实配方扣库存验证通过：`ingredient_id=2` 扣 `0.40`，`ingredient_id=3` 扣 `4.00`，`ingredient_id=8` 扣 `0.30`
  - `PUT /restaurant/order/cancel/3` 成功，订单状态 `2 -> 3`，库存完整回滚
  - 现成库存不足测试单 `orderId=2` 完成时报错 `食材ID[1]库存不足，需要 30.000，扣减失败，订单已回滚`，且订单状态保持 `1`、库存未脏写
- ✅ **2026-06-20 页面侧快速复核已完成**：
  - 以 `manager/admin123` 登录后，可进入 `餐厅管理 / 订单管理`
  - 订单列表可正常显示 D6 测试单，状态为“已下单”的订单显示“完成”按钮，已退单订单不再显示业务按钮
  - “模拟下单”按钮可正常拉起弹窗
  - 点击“完成”按钮后，会先弹出二次确认提示：`确认完成订单"... "吗？完成后将按配方扣减库存。`
  - 本轮已确认页面入口、按钮显隐、确认交互与后端链路基本对齐；更细的页面完善项继续归 D7 收口
- ✅ 为适配本机运行环境，`ruoyi-admin/src/main/resources/logback.xml` 的日志路径已改为相对目录 `./logs`，避免 `D:/ruoyi/logs` 权限问题导致 `jar` 启动后立即退出

**完成标志：** ✅ 完成订单后库存按配方正确扣减；库存不足时事务回滚并提示

### D7 订单与库存前端联调 [❌ 待办]

- ⏳ 页面侧复核 `order/index.vue`：模拟下单弹窗、完成/退单按钮、状态显隐、错误提示与接口返回一致
- ❌ 订单列表筛选体验完善（状态筛选/时间筛选/搜索）
- ❌ 订单详情展示完善（订单基本信息 + 明细表 + 操作按钮）
- ❌ 在现有 `inventory/stockIn` 页面骨架上补齐库存预警高亮、入库业务交互
- ❌ 页面级联调订单与库存变化，补截图/演示话术

**完成标志：** 主业务闭环可在页面上稳定演示，交互与接口结果一致

### D8 统计接口 [❌ 待办]

- ❌ 今日营收统计（`SUM(total_amount) WHERE DATE(order_time) = CURDATE() AND status = 2`）
- ❌ 今日订单数统计
- ❌ 近 7 天营收趋势（按天 GROUP BY）
- ❌ 销量 Top10（按 dish_id GROUP BY 数量）
- ❌ 库存预警列表（`stock < safety_stock`）
- ❌ 看板聚合接口（一次返回所有卡片数据）

**完成标志：** 看板接口返回完整数据

### D9 看板前端 [❌ 待办]

- ❌ 数字卡片（今日营收/今日订单数/库存预警数/菜品数）
- ❌ 近 7 天折线图（ECharts）
- ❌ 销量 Top10 柱状图
- ❌ 库存预警表格
- ❌ 调整系统标题为"文瀛餐厅管理系统"（`ruoyi-ui/src/settings.js` + `ruoyi-ui/src/layout/components/Sidebar/Logo.vue`）

**完成标志：** 核心页面全部可见且可演示

### D10 联调、演示数据与答辩准备 [❌ 待办]

- ❌ 走完整业务链路：新建食材→入库→建配方→模拟下单→完成订单→看板变化
- ❌ 修复主流程问题
- ❌ 灌入近 7 天订单演示数据（约 200 单，覆盖所有菜品）
- ❌ 检查三角色权限隔离（admin 全部 / manager 无食材管理 / stock_keeper 无订单操作）
- ❌ 完成部署说明（02_TRD 已含，需补演示截图）
- ❌ 完成用户说明
- ❌ 完成演示脚本（3-5 分钟流程）
- ❌ 准备答辩 PPT
- ❌ 至少彩排两次

**完成标志：** 软件、文档、演示材料齐全

---

## 6. 风险控制

- 如果配方页太复杂，简化为普通表单逐行录入
- 如果统计 SQL 时间过长，先保证 4 个核心指标
- 如果环境卡住，先优先解决 JDK、Maven、Node 版本
- 如果时间不足，优先保住"配方 → 入库 → 下单 → 扣库存 → 看板"链路（这是 D4-D7 的核心）
- 库存扣减必须用 `UPDATE ... WHERE stock >= ?` 模式，禁止先 SELECT 再 UPDATE（避免并发超扣）

---

## 7. 最终验收清单

- [ ] 三角色登录正常
- [ ] 菜品增删改查正常
- [ ] 食材增删改查正常（新增时自动建库存）
- [ ] 配方维护正常
- [ ] 入库后库存增加
- [x] 模拟下单成功（D6 后端接口实测）
- [x] **完成订单后库存按配方扣减**（D6 后端接口实测）
- [x] **库存不足时阻止完成订单并事务回滚**（D6 后端接口实测）
- [x] 退单后库存回滚（D6 后端接口实测）
- [ ] 看板图表正常显示
- [ ] 安装说明可复现运行
- [ ] 演示数据完整可演示

---

## 8. 推进顺序建议

**D4、D5、D6 已完成**（菜品分类 UX 修正、上下架、配方入口；食材/配方/订单明细模型、自动建库存、配方维护页面；订单核心逻辑：模拟下单/完成订单/退单、按配方扣库存+事务回滚、前端业务按钮；D6 后端接口实测已完成）。

下一步按 **D7（订单与库存前端联调）→ D8（统计接口）→ D9（看板前端）→ D10（联调与答辩）** 顺序推进。

关键提醒：

- **D6 已完成**：TOrderServiceImpl 已实现 mockOrder/completeOrder/cancelOrder；TInventoryMapper 已加 deductStock/addStock；TOrder Domain 已加 orderItems 字段；前端 order/index.vue 已加模拟下单弹窗和完成/退单按钮
- **D6 已实测通过**：`mockOrder / completeOrder / cancelOrder` 与库存不足回滚均已用真实数据跑通，当前更值得推进的是页面侧复核与交互收口
- **D7 时**：清理剩余 4 模块（ingredient/inventory/stockIn/order）表单中的 delFlag 字段，将 ingredientId 显示为食材名称
