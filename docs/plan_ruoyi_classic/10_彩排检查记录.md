# 文瀛餐厅管理系统 — 彩排检查记录

## 1. 记录目标

- 以真实运行环境沉淀 D10 答辩证据，覆盖品牌展示、角色隔离、主链页面和看板结果。
- 区分“已完成的真实页面核验”和“仍待现场口播压时长的人工彩排”，避免记录口径夸大。
- 为答辩前最后一次手工演示提供可直接照着走的核验底稿。

## 2. 核验环境

| 项目 | 内容 |
|------|------|
| 运行根目录 | `G:\restaurant_manager\RuoYi-Vue` |
| 前端地址 | `http://127.0.0.1` |
| 后端地址 | `http://127.0.0.1:8080` |
| 经理账号 | `manager/admin123` |
| 库管账号 | `stockkeeper/admin123` |
| 截图目录 | `docs/plan_ruoyi_classic/assets/d10/` |
| 证据方式 | 真实 HTTP/API + 本地 Edge/CDP 真页面截图 + 构建验证 |

补充说明：

- 当前 API 会话下，应用内 Browser / Computer Use 官方链路仍返回 `missing field sandboxPolicy`，因此本轮未使用官方应用内自动化。
- 为了不阻塞收尾，本轮改用本机可用的 Edge/CDP 页面探针补齐真实截图；截图文件现已落到 `assets/d10/`。

## 3. 第一轮核验（真实页面截图回收）

| 项目 | 记录 |
|------|------|
| 时间 | 2026-06-21 |
| 账号 | `manager/admin123`、`stockkeeper/admin123` |
| 路径 | 登录页 -> 首页 -> 配方页 -> 入库页 -> 订单页 -> 看板页 -> manager 菜单 -> stockkeeper 菜单 |
| 结果 | 8 张真实页面截图已补齐 |
| 发现问题 | 早期脚本产出的 `02/07/08` 与其他截图重复，且首页图一度回落到登录页 |
| 修复状态 | 已重新抓取 `02-dashboard.png`、`07-role-manager.png`、`08-role-stockkeeper.png`，8 张截图现均为独立证据 |

检查点：

- [x] `01-login.png` 显示“文瀛餐厅管理系统”登录页。
- [x] `02-dashboard.png` 为真实首页，不再与登录页重复。
- [x] `03-recipe.png` 展示 manager 可进入的配方页面。
- [x] `04-stock-in.png` 展示 stockkeeper 可进入的入库管理页面。
- [x] `05-order.png` 拍到订单页业务入口：`模拟下单 / 详情 / 完成 / 退单`。
- [x] `06-stats.png` 拍到 4 个卡片、2 个图表、预警表格。
- [x] `07-role-manager.png`、`08-role-stockkeeper.png` 已单独体现角色菜单隔离。
- [x] 截图核对时未再出现“若依后台管理框架 / 访问码云 / 访问主页 / 若依官网”等公开展示面残留。

## 4. 第二轮核验（脚本对照与实时数据复核）

| 项目 | 记录 |
|------|------|
| 时间 | 2026-06-21 |
| 账号 | `manager/admin123` |
| 路径 | 对照 `07_答辩演示脚本.md` 复核首页、配方、订单、看板与角色说明 |
| 结果 | 演示脚本与当前系统页面、菜单和数据口径一致 |
| 发现问题 | 旧文档中“今日营收/订单数为 0”的说明已过期 |
| 修复状态 | 已刷新真实数据口径并同步修正文档 |

检查点：

- [x] `manager` 路由包含 数据看板、菜品分类、菜品管理、库存管理、菜品配方、订单管理。
- [x] `stockkeeper` 路由只包含 食材档案、库存管理、入库管理，不含订单管理和数据看板。
- [x] `/restaurant/stats/dashboard` 返回实时数据：`todayRevenue=15.00`、`todayOrderCount=1`、`dishCount=56`、`warningCount=4`。
- [x] 最新订单核验到 `orderId=218` 已完成，`completeTime=2026-06-21`，因此答辩时已不需要再解释“今日营收为 0”。
- [x] `/restaurant/order/list` 最新记录显示已完成、已下单、已退单三种状态并存，适合演示状态流转。
- [x] `/restaurant/recipe/list/{dishId}` 仍可正常查看配方；前端保存入口继续受 `restaurant:recipe:save` 控制。
- [ ] 尚未单独做一轮“现场口播计时版”人工压时长彩排，本文件当前记录的是页面核验与脚本对照证据。

## 5. 当前证据结论

本轮已经完成的收尾证据如下：

- 前后端服务可访问：`/captchaImage` 返回 `code=200`，登录链路正常。
- 真实页面截图已补齐：`assets/d10/01-login.png` 至 `08-role-stockkeeper.png` 全部存在。
- 品牌展示已统一：登录页、首页、侧边栏、订单页、看板页截图中未见若依模板外链和文案残留。
- 角色隔离已在页面级证据中体现：manager 可见配方/订单/看板，stockkeeper 不可见。
- 看板口径已刷新为当前真实状态：`todayRevenue=15.00`、`todayOrderCount=1`，对应当日已完成订单 `orderId=218`。
- 构建与接口兜底链路可继续作为答辩前复核手段。

## 6. 仍待补的唯一人工项

当前还未在本文件中形成“至少两次现场口播压时长”的人工计时记录，因此这一项仍待答辩前手工补齐。建议按 `07_答辩演示脚本.md` 再做两次人工演示，并补记录以下字段：

- 开始/结束时间
- 口播总时长
- 是否现场新建演示单
- 是否展示完成订单与看板变化
- 角色切换是否在 30 秒内完成
- 临场卡顿点与规避话术

## 7. 最终判断

- 软件、数据、权限、品牌和真实页面截图证据已经具备答辩展示条件。
- 当前没有发现阻断性问题；官方应用内 Browser / Computer Use 不可用不影响本轮手工收尾。
- 剩余最小尾项只有“人工压时长彩排记录”这一项，补完后 D10 材料侧会更完整。
